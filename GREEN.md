# 🌿 Carbon Audit Report - aura-cli

## Carbon Efficiency Score: D

Generated on: 2026-02-11 03:05:02

### Bloat Analysis

**Large Files (>50MB):**
✓ No large files detected

**Bloated Directories:**
- .venv: 111.62 MB
__pycache__: 0.05 MB

**Total Bloat:** 111.7 MB

### Complexity Analysis

**Big O Complexity Analysis:**
- `render_title_panel()`: **O(n)** - character iteration with modulo styling
- `display_header()`: **O(1)** iterations but **O(k·t)** wall-clock time where k=4 steps, t=0.8s blocking sleeps
- Character-by-character Text.append() creates intermediate objects: **O(n)** memory allocations

**Carbon-Neutral Refactor:** Replace blocking `time.sleep()` calls with async/await or eliminate animations entirely (saves ~3.2s CPU cycles per invocation). Batch string styling using slicing: `title_text.append(label, style=f"bold {palette[0]}")` instead of per-character loops. Cache rendered Panel objects in module-level dict to avoid redundant Rich rendering overhead on repeated calls.

### Recommendations

1. **File Management**: Consider removing or compressing files over 50MB
2. **Directory Cleanup**: Archive or remove node_modules and .venv before deployment
3. **Code Optimization**: Implement the refactoring suggestions above
4. **Monitoring**: Track carbon score in your CI/CD pipeline

### Carbon Score Legend

- **A**: Excellent (≥90% efficiency)
- **B**: Good (≥80% efficiency)
- **C**: Average (≥70% efficiency)
- **D**: Below Average (≥60% efficiency)
- **E**: Poor (≥50% efficiency)
- **F**: Critical (<50% efficiency)

---
*Report generated by Aura CLI - Carbon Efficiency Auditor*
